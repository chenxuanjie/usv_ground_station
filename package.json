{
  "name": "usv_ground_station-web",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "clean:web": "rm -rf dist",
    "build:web": "rm -rf dist && mkdir -p dist/js && babel js --out-dir dist/js --extensions \".js\" --ignore \"js/lib/**\" --source-maps"
  },
  "devDependencies": {
    "@babel/cli": "^7.26.4",
    "@babel/core": "^7.26.7",
    "@babel/preset-env": "^7.26.7",
    "@babel/preset-react": "^7.26.3"
  }
}

